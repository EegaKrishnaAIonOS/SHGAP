/**
 * Shared TypeScript types consumed by both the web app and the NestJS
 * services. Grows alongside the domain as each module is built out
 * (T06+) — kept empty at scaffold time (T03).
 */

export const SHARED_TYPES_PACKAGE_VERSION = "0.1.0";
